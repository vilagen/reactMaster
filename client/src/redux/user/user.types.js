export const UserActionTypes = {
  SET_CURRENT_USER: 'SET_CURRENT_USER'
}

// so this helps us prevents typos/errors, since we could just import this instead of typing in 'SET_CURRENT_USER'
// sounds like it's not needed, but does help.